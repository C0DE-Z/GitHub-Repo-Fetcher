{
  "name": "github-repo-fetcher",
  "version": "1.0.0",
  "description": "Fetch GitHub repositories and READMEs for a given profile",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "test": "jest",
    "test:watch": "jest --watch",
    "example": "npx ts-node src/example.ts"
  },
  "keywords": [
    "github",
    "api",
    "readme",
    "repositories"
  ],
  "dependencies": {
    "axios": "^1.4.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^18.16.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "ts-node": "^10.9.2",
    "typescript": "^4.9.5"
  }
}
